name: Run linter

on: [push]

jobs:
  lint:
    runs-on: ubuntu-latest
    name: Lint code
    steps:
    - uses: Silleellie/pylint-github-action@v2
      with:
        lint-path: |
          src/mepo
          src/mepo3
        python-version: 3.9
      timeout-minutes: 5
