[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "mepo"
version = "v1.59.0"
authors = [
    {name = "GMAO SI Team", email = "gmao.siteam@nasa.gov"},
]
description = "Add description here"
requires-python = ">= 3.9"
license = {file = "LICENSE"}
readme = "README.md"
dependencies = [
    "pyyaml>=6.0.1",
]

[project.urls]
Homepage = "https://github.com/GEOS-ESM/mepo"

[project.scripts]
mepo = "mepo.main:main"

[tool.setuptools]
include-package-data = true
